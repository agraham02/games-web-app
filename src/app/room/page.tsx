"use client";

/**
 * The way in. No code yet — create a room, or type one you were given.
 */

import { RoomScreen } from "@/room/RoomScreen";

export default function RoomEntryPage() {
  return <RoomScreen />;
}
